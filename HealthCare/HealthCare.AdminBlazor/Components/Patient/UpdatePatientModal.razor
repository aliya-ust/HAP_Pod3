@using HealthCare.Shared.DTOs.Patient
@using HealthCare.AdminBlazor.Services

@inject PatientAdminService PatientService

@if (Visible)
{
    <div class="modal-overlay">

        <div class="modal-box">

            <div class="modal-header">

                @if (!string.IsNullOrEmpty(errorMessage))
                {
                    <div class="error-message">
                        @errorMessage
                    </div>
                }

                @if (!string.IsNullOrEmpty(successMessage))
                {
                    <div class="success-message">
                        @successMessage
                    </div>
                }

                <h3>Update Patient</h3>

                <button class="close-btn"
                        @onclick="Close">
                    ✖
                </button>

            </div>

            <EditForm Model="model"
                      OnValidSubmit="SavePatient">

                <DataAnnotationsValidator />

                <div class="form-group">

                    <label>Full Name</label>

                    <InputText class="form-control"
                               @bind-Value="model.FullName" />

                    <ValidationMessage For="@(() => model.FullName)" />

                </div>

                <div class="form-group">

                    <label>Email</label>

                    <InputText class="form-control"
                               @bind-Value="model.Email" />

                    <ValidationMessage For="@(() => model.Email)" />

                </div>

                <div class="form-group">

                    <label>Phone Number</label>

                    <InputText class="form-control"
                               @bind-Value="model.PhoneNumber" />

                    <ValidationMessage For="@(() => model.PhoneNumber)" />

                </div>

                <div class="form-group">

                    <label>Gender</label>

                    <InputSelect class="form-control"
                                 @bind-Value="model.Gender">

                        <option>Male</option>
                        <option>Female</option>
                        <option>Other</option>

                    </InputSelect>

                    <ValidationMessage For="@(() => model.Gender)" />

                </div>

                <div class="form-group">

                    <label>Insurance Id</label>

                    <InputText class="form-control"
                               @bind-Value="model.InsuranceId" />

                    <ValidationMessage For="@(() => model.InsuranceId)" />

                </div>

                <div class="button-section">

                    <button type="button"
                            class="cancel-btn"
                            @onclick="Close">
                        Cancel
                    </button>

                    <button type="submit"
                            class="save-btn">
                        Save Changes
                    </button>

                </div>

            </EditForm>

        </div>

    </div>
}

@code {

    [Parameter]
    public PatientListDto? Patient { get; set; }

    [Parameter]
    public bool Visible { get; set; }

    [Parameter]
    public EventCallback OnClose { get; set; }

    [Parameter]
    public EventCallback OnSaved { get; set; }

    private UpdatePatientDto model = new();
    private string errorMessage = string.Empty;
    private string successMessage = string.Empty;


    protected override void OnParametersSet()
    {
        if (Patient != null)
        {
            model = new UpdatePatientDto
            {
                FullName = Patient.FullName,
                Email = Patient.Email,
                PhoneNumber = Patient.PhoneNumber,
                Gender = Patient.Gender,
                InsuranceId = Patient.InsuranceId
            };
        }
    }

    private async Task SavePatient()
    {
        if (Patient == null)
            return;

        errorMessage = string.Empty;
        successMessage = string.Empty;

        try
        {
            await PatientService.UpdatePatient(
                Patient.PatientId,
                model);

            successMessage = "Patient updated successfully";

            await OnSaved.InvokeAsync();
            await OnClose.InvokeAsync();
        }
        catch (HttpRequestException)
        {
            errorMessage = "Email already exists";
        }
        catch (Exception)
        {
            errorMessage = "Something went wrong";
        }
    }

    private async Task Close()
    {
        await OnClose.InvokeAsync();
    }
}

<style>

    .modal-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,.45);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 1000;
    }

    .modal-box {
        width: 500px;
        background: white;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 15px 35px rgba(0,0,0,.2);
    }

    .modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 25px;
    }

        .modal-header h3 {
            margin: 0;
            color: #0f172a;
        }

    .close-btn {
        border: none;
        background: none;
        font-size: 22px;
        cursor: pointer;
    }

    .form-group {
        margin-bottom: 22px;
    }

        .form-group label {
            display: block;
            width: 100%;
            text-align: left;
            margin-bottom: 8px;
            font-weight: 600;
            color: #334155;
            font-size: 15px;
        }

    .form-control {
        width: 100%;
        padding: 14px;
        border: 1px solid #d1d5db;
        border-radius: 12px;
        box-sizing: border-box;
    }

        .form-control:focus {
            outline: none;
            border-color: #2563eb;
            box-shadow: 0 0 0 4px rgba(37,99,235,.15);
        }

    .button-section {
        display: flex;
        gap: 20px;
        margin-top: 35px;
    }

        .button-section button {
            flex: 1; /* both take equal width */
            height: 55px;
            margin: 0 !important;
            padding: 0 !important;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            box-sizing: border-box;
            vertical-align: middle;
        }

    .cancel-btn {
        background: #e2e8f0;
        color: #1e293b;
    }

    .save-btn {
        background: linear-gradient(135deg,#0f172a,#1e3a8a,#2563eb);
        color: white;
    }

    .modal-box {
        width: 500px;
        background: white;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 15px 35px rgba(0,0,0,.2);
        text-align: left;
    }

    .filter-dropdown {
        appearance: none;
        -webkit-appearance: none;
        -moz-appearance: none;
        background: url("data:image/svg+xml;utf8,<svg fill='%232563eb' height='20' viewBox='0 0 24 24' width='20' xmlns='http://www.w3.org/2000/svg'><path d='M7 10l5 5 5-5z'/></svg>") no-repeat right 12px center;
        padding-right: 40px;
        background-color: white;
    }

</style>